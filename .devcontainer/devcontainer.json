{
    "image": "mcr.microsoft.com/devcontainers/ruby:3.0",
    "dockerComposeFile": "../docker-compose.yml",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nodejs && bin/setup ruby",
    "postStartCommand": "make services"
}
