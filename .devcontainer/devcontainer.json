{
	"image": "mcr.microsoft.com/devcontainers/universal:linux",
    // "dockerComposeFile": "../docker-compose.yml",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y proj-bin postgresql-client && rvm install 3.0.4 && nvm install && cp docker-compose.override.yml.example docker-compose.override.yml && make services && bin/setup ruby",
	// cp docker-compose.override.yml.example docker-compose.override.yml
	// "postStartCommand": "make services",
	"forwardPorts": [3000],
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {}
	}
}
